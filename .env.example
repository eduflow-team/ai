# === PostgreSQL (Langflow 내부 DB, compose 전용) ===
POSTGRES_USER=langflow
POSTGRES_PASSWORD=langflow
POSTGRES_DB=langflow

# === Langflow API Key (로컬 curl 테스트용) ===
# UI: Settings → API Keys 에서 본인 키 발급 후 .env 에만 입력 (팀 공유 금지)
# LANGFLOW_API_KEY=
