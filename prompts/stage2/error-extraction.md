# Stage 2 — Error Extraction Prompt (deprecated)

> **v2(plan-first)부터 사용하지 않음.**  
> 대체: `error-plan.md` (Planner) + `stage2_plan_formatter` (Custom Component)

v1에서 `Prompt-We0Ob`는 OpenAI **사후 오류 추출**용이었으나,  
v2에서는 동일 노드 ID(`Prompt-We0Ob`)가 **error_plan_prompt**로 재사용됩니다.

이 파일은 v1 히스토리 참고용으로만 유지합니다.
